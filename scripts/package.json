{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:focused": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "dotenv": "^17.2.3",
    "pg": "^8.17.2"
  }
}
